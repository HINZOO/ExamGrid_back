<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gridvue.backend.mapper.LocationMapper">
  <select id="findAll">
    SELECT * FROM location 
  </select>
  <select id="findByLocParent">
    SELECT * FROM location WHERE loc_parent=#{loc_parent}
  </select>
</mapper>